node_modules/
.env

# Logged-in browser profiles — never commit session state
.profiles/

# Raw capture + evidence can contain large files; keep out of git by default
data/raw/
evidence/

# Playwright output
playwright-report/
test-results/
